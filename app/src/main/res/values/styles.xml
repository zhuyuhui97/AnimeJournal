<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="actionBarStyle">@style/Widget.StyledActionBar</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Widget.StyledActionBar" parent="@style/Widget.AppCompat.ActionBar">
        <item name="background">@color/deep</item>

    </style>

</resources>
